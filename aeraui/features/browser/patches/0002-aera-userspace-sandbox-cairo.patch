diff --git a/Source/WTF/wtf/PlatformWPE.cmake b/Source/WTF/wtf/PlatformWPE.cmake
--- a/Source/WTF/wtf/PlatformWPE.cmake
+++ b/Source/WTF/wtf/PlatformWPE.cmake
@@ -55,6 +55,7 @@
     glib/RunLoopSourcePriority.h
     glib/Sandbox.h
+    glib/AERASandbox.h
diff --git a/Source/WTF/wtf/glib/AERASandbox.h b/Source/WTF/wtf/glib/AERASandbox.h
new file mode 100644
--- /dev/null
+++ b/Source/WTF/wtf/glib/AERASandbox.h
@@ -0,0 +1,35 @@
+// SPDX-License-Identifier: Apache-2.0
+#pragma once
+#include <cerrno>
+#include <cstdlib>
+#include <initializer_list>
+#include <linux/capability.h>
+#include <sys/prctl.h>
+#include <sys/stat.h>
+#include <sys/statfs.h>
+#include <sys/statvfs.h>
+#include <sys/syscall.h>
+#include <unistd.h>
+namespace aera_sandbox {
+inline bool Validate() {
+  if (getuid() != 99090 || geteuid() != 99090 || getgid() != 99090 || getegid() != 99090 ||
+      getgroups(0, nullptr) != 0 || prctl(PR_GET_NO_NEW_PRIVS, 0, 0, 0, 0) != 1 ||
+      prctl(PR_GET_SECCOMP, 0, 0, 0, 0) != 2 || getenv("WEBKIT_DISABLE_SANDBOX_THIS_IS_DANGEROUS")) return false;
+  struct __user_cap_header_struct h = {_LINUX_CAPABILITY_VERSION_3, 0};
+  struct __user_cap_data_struct c[2] = {};
+  if (syscall(SYS_capget, &h, c) || c[0].effective || c[1].effective || c[0].permitted ||
+      c[1].permitted || c[0].inheritable || c[1].inheritable) return false;
+  for (int i = 0; i <= 40; ++i) if (prctl(PR_CAPBSET_READ, i, 0, 0, 0) != 0) return false;
+  struct stat root = {}; struct statfs fs = {}; struct statvfs flags = {};
+  if (stat("/", &root) || root.st_uid || (root.st_mode & 0777) != 0755 || statfs("/", &fs) ||
+      (fs.f_type != 0x01021994 && fs.f_type != 0x858458f6) || statvfs("/", &flags) || !(flags.f_flag & ST_RDONLY)) return false;
+  for (const char *path : {"/data", "/dev/block", "/sys", "/proc/1"}) {
+    struct stat info = {}; if (lstat(path, &info) != -1 || errno != ENOENT) return false;
+  }
+  return prctl(PR_SET_DUMPABLE, 0, 0, 0, 0) == 0;
+}
+}
diff --git a/Source/WTF/wtf/glib/Sandbox.cpp b/Source/WTF/wtf/glib/Sandbox.cpp
--- a/Source/WTF/wtf/glib/Sandbox.cpp
+++ b/Source/WTF/wtf/glib/Sandbox.cpp
@@ -25,6 +25,7 @@
 #include "config.h"
 #include <wtf/glib/Sandbox.h>
+#include "AERASandbox.h"
@@ -82,6 +83,14 @@ bool shouldUseBubblewrap()
 {
+#if PLATFORM(WPE)
+    if (getuid() == 99090) {
+        if (!aera_sandbox::Validate())
+            abort();
+        return false;
+    }
+#endif
diff --git a/Source/WebKit/NetworkProcess/soup/NetworkProcessMainSoup.cpp b/Source/WebKit/NetworkProcess/soup/NetworkProcessMainSoup.cpp
--- a/Source/WebKit/NetworkProcess/soup/NetworkProcessMainSoup.cpp
+++ b/Source/WebKit/NetworkProcess/soup/NetworkProcessMainSoup.cpp
@@ -31,6 +31,7 @@
 #include <WebCore/NetworkStorageSession.h>
+#include <wtf/glib/AERASandbox.h>
@@ -63,6 +64,8 @@ int NetworkProcessMain(int argc, char** argv)
 {
+    if (getuid() == 99090 && !aera_sandbox::Validate())
+        return 78;
diff --git a/Source/WebKit/WebProcess/wpe/WebProcessMainWPE.cpp b/Source/WebKit/WebProcess/wpe/WebProcessMainWPE.cpp
--- a/Source/WebKit/WebProcess/wpe/WebProcessMainWPE.cpp
+++ b/Source/WebKit/WebProcess/wpe/WebProcessMainWPE.cpp
@@ -30,6 +30,7 @@
 #include <glib.h>
+#include <wtf/glib/AERASandbox.h>
@@ -97,6 +98,8 @@ int WebProcessMain(int argc, char** argv)
 {
+    if (getuid() == 99090 && !aera_sandbox::Validate())
+        return 78;
diff --git a/Source/WebKit/UIProcess/wpe/WebPreferencesWPE.cpp b/Source/WebKit/UIProcess/wpe/WebPreferencesWPE.cpp
--- a/Source/WebKit/UIProcess/wpe/WebPreferencesWPE.cpp
+++ b/Source/WebKit/UIProcess/wpe/WebPreferencesWPE.cpp
@@ -26,9 +26,18 @@
 #include "WebPreferences.h"
+#include <unistd.h>
@@
 void WebPreferences::platformInitializeStore()
 {
+    if (getuid() == 99090) {
+        setAcceleratedCompositingEnabled(false);
+        setForceCompositingMode(false);
+        setThreadedScrollingEnabled(false);
+        return;
+    }
diff --git a/Source/WebKit/WebProcess/glib/WebProcessGLib.cpp b/Source/WebKit/WebProcess/glib/WebProcessGLib.cpp
--- a/Source/WebKit/WebProcess/glib/WebProcessGLib.cpp
+++ b/Source/WebKit/WebProcess/glib/WebProcessGLib.cpp
@@ -49,6 +49,7 @@
 #include <WebCore/MemoryCache.h>
+#include <unistd.h>
@@ -222,7 +223,8 @@
-    initializePlatformDisplayIfNeeded();
+    if (getuid() != 99090)
+        initializePlatformDisplayIfNeeded();
diff --git a/Source/WebKit/WebProcess/WebPage/CoordinatedGraphics/DrawingAreaCoordinatedGraphics.cpp b/Source/WebKit/WebProcess/WebPage/CoordinatedGraphics/DrawingAreaCoordinatedGraphics.cpp
--- a/Source/WebKit/WebProcess/WebPage/CoordinatedGraphics/DrawingAreaCoordinatedGraphics.cpp
+++ b/Source/WebKit/WebProcess/WebPage/CoordinatedGraphics/DrawingAreaCoordinatedGraphics.cpp
@@ -46,6 +46,7 @@
 #include <wtf/SetForScope.h>
+#include <unistd.h>
@@ -266,6 +267,8 @@ bool DrawingAreaCoordinatedGraphics::enterAcceleratedCompositingModeIfNeeded()
     if (!m_alwaysUseCompositing) {
 #if PLATFORM(WPE)
+        if (getuid() == 99090)
+            return false;
